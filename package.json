{
  "name": "woodhouse-spa-helper",
  "version": "0.1.0",
  "private": true,
  "description": "Free helper that shows open appointment times at Woodhouse spas. Read-only: it never books anything.",
  "scripts": {
    "locations": "node scripts/enumerate-locations.js",
    "pull": "node scripts/pull-location.js",
    "upload": "node scripts/upload-blob.js",
    "web": "node scripts/dev-web.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@vercel/blob": "^2.6.1"
  }
}
